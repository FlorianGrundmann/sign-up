import 'package:flutter/material.dart';

class EmailSignUpPage extends StatelessWidget {
  const EmailSignUpPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Email Sign-Up Page.'),
    );
  }
}
